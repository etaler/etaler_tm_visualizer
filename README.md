## Etaler TM visualizer

This is the original Etaler visualizer example but moved to it's own repo due to issues with [Read the Docs](https://readthedocs.org/).

To build the visualizer:

```
mkdir build
cd build
cmake ..
make
```